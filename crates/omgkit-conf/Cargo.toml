[package]
name = "omgkit-conf"
description = "Deterministic 3D conformer generation for omgkit: distance geometry without the dice"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true
homepage.workspace = true
readme.workspace = true

[dependencies]
omgkit-core = { path = "../omgkit-core", version = "0.0.3" }
omgkit-chem = { path = "../omgkit-chem", version = "0.0.3" }
# `pipeline::conformer_for` 要感知双键顺反、要规范秩(补显式氢的插入顺序按它定),
# 两样都在 L1。omgkit-io 只依赖 omgkit-core,不成环。
omgkit-io = { path = "../omgkit-io", version = "0.0.3" }

[dev-dependencies]
# 判官读 RDKit 导出的界矩阵 JSONL
serde_json = "1"

[lints]
workspace = true
