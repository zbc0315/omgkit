/target/
**/*.rs.bk

# **`Cargo.lock` 入库。**
#
# 库 crate 传统上不入锁文件(下游本来就会忽略它),但那让 **CI 每次从头解析依赖** ——
# 于是上游一次撤版就能把一个毫不相干的提交打红。
#
# 这不是假设:2026-08-20,`arrayref` 的 0.3.5–0.3.9 **全部被撤版**,
# 而 `tiny-skia`(depict 的可选位图输出)要求 `^0.3.6` —— 没有任何未撤版的版本
# 能满足它。本地照常编译(锁文件里的撤版版本仍然可用),CI 21 秒就挂在
# `failed to select a version for the requirement`。
#
# 锁文件入库不影响下游(他们仍然自己解析),但让 CI 可复现 ——
# **一道会被上游随机打红的闸,和没有闸差不多。**

# 差分测试的基准:体积大且可复现,默认不入库。
#
# **例外的判据是"没标 #[ignore] 的测试要用到它"**,不是"文件名叫什么"。
# 默认 `cargo test` 会跑到的基准必须入库,否则新克隆下来测试直接红。
# 按文件名猜过两次都猜漏了:先漏了 `smoke.matches.tsv`(放行规则写成了
# `smoke.*.jsonl`,按后缀挡掉了 `.tsv`),又漏了 `smarts.jsonl`(它压根不叫
# smoke,却被一条非 ignore 的测试硬编码引用)。
#
# 改动这一段之后要验的不是"规则看着对",而是**把仓库克隆到别处跑一遍
# `cargo test`** —— 那才是"开箱即跑"的判据。当前入库合计 1658 KB(重量出来的;先前写的 680K 早就过期了,
# 850K 是我拿旧数加 164K 算的,同样不作数)。
/harness/baseline/*
!/harness/baseline/smoke.*
!/harness/baseline/smarts.jsonl
# 匹配差分的大语料基准(164 K)。`matches_large` 解禁之后默认 `cargo test`
# 会跑到它 —— 按上面那条规矩就必须入库。
!/harness/baseline/matches.tsv
/harness/corpus/chembl*.smi
/harness/corpus/*.sdf
/harness/report/

# maturin build 的产物,复现命令写在 README
/dist/

__pycache__/
*.py[cod]
.venv/
.DS_Store
docs/dev/design-full.md
docs/dev/correctness-full.md
site/
.venv-docs/

# 送审 subagent 的临时 worktree(isolation: worktree),不该进版本库
.claude/
